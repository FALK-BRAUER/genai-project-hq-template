# learnings/

Accumulated knowledge extracted from projects and sessions. Feeds back into templates and global config.

- `per-project/` — learnings scoped to a specific project
- Top-level `.md` files — cross-project learnings and patterns

Added automatically by `scripts/sync-learnings.sh` or the `/sync` skill. Do not delete — these inform future sessions.
