# library/hooks/

Claude Code hook scripts. Hooks run automatically on session events (start, tool use, etc.).

- `claude-code/` — hooks for Claude Code CLI

Installed into `~/.claude/hooks/` via `scripts/install-global.sh`.
