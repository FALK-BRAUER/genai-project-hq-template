{
  "event": "PreSession",
  "description": "Inject today's date at session start",
  "hooks": [
    {
      "hooks": [{
        "type": "command",
        "command": "python3 -c \"from datetime import datetime; now=datetime.now(); print(f'Today: {now.strftime(\\'%A, %B %d, %Y\\')}')\"",
        "blocking": false
      }]
    }
  ]
}
