# library/commands/

Slash command definitions shared across projects.

- `claude-code/` — commands for Claude Code CLI (loaded into `~/.claude/commands/`)

Each command is a `.md` file with a prompt template invoked via `/command-name`.
