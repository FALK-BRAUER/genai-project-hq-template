# global-config/claude-code/

Global Claude Code CLI configuration. Synced to `~/.claude/` on each machine.

- `settings.json` — global settings (hooks, permissions, model preferences)
- `mcp-servers.json` — MCP server definitions shared across all projects
