# global-config/shell/

Shell helpers and aliases sourced into the terminal environment.

- `claude-aliases.sh` — shorthand aliases for common Claude Code commands
- `hqi` — launcher script for the hqi (headless quick invoke) workflow
