# macOS
.DS_Store
.AppleDouble
.LSOverride

# Logs — generated by scheduled jobs, no need to track
tools/logs/

# Personal settings — machine-specific
.claude/settings.local.json
CLAUDE.local.md

# Cost data — personal spending, don't commit
costs/

# Session context — not useful for others
zz_handoffs/

# Secrets — never commit
.env
.env.local
*.pem
*.key

# Node
node_modules/
